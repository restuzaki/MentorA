import 'package:flutter/widgets.dart';

class InputMapelScreen extends StatelessWidget {
  const InputMapelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
