import 'package:flutter/cupertino.dart';

class ExamTeacherScreen extends StatelessWidget {
  const ExamTeacherScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
